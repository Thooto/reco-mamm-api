# API "Reconstruction Mammaire"
The API for the "Reconstruction Mammaire" website

# Start it
Fill the .env.dist file with your own environment variables, remove the .dist suffix then run: npm install && npm start